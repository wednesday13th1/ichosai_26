import * as THREE from 'three';
export const ease=t=>t*t*(3-2*t);
export function around(object,index,total,radius=3,y=0){const angle=index/total*Math.PI*2;object.position.set(Math.sin(angle)*radius,y,-Math.cos(angle)*radius);object.rotation.y=-angle;object.userData.base=object.position.clone();object.userData.angle=angle;return object;}
export function makeCardTexture(suit='♥',value='Q',palette={paper:'#ead8b4',ink:'#160506',red:'#a61924'}){const canvas=document.createElement('canvas');canvas.width=256;canvas.height=356;const x=canvas.getContext('2d');x.fillStyle=palette.paper;x.fillRect(0,0,256,356);for(let i=0;i<90;i++){const px=(i*83)%251,py=(i*137)%349;x.fillStyle=`rgba(70,42,25,${.012+(i%5)*.004})`;x.fillRect(px,py,1+i%2,1+i%3);}x.strokeStyle='#8f7950';x.lineWidth=6;x.strokeRect(5,5,246,346);x.fillStyle=suit==='♥'||suit==='♦'?palette.red:palette.ink;x.font='bold 44px Georgia';x.textAlign='left';x.fillText(value,18,50);x.fillText(suit,18,96);x.font='92px Georgia';x.textAlign='center';x.fillText(suit,128,220);const texture=new THREE.CanvasTexture(canvas);texture.colorSpace=THREE.SRGBColorSpace;return texture;}
export function disposeRoot(root){const geometries=new Set(),materials=new Set();root.traverse(o=>{if(o.geometry)geometries.add(o.geometry);if(o.material)materials.add(o.material);});geometries.forEach(g=>g.dispose());materials.forEach(m=>{m.map?.dispose();m.dispose();});}

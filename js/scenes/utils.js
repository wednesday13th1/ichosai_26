import * as THREE from 'three';
export function polarPosition(angleDeg,radius,y){const angle=THREE.MathUtils.degToRad(angleDeg);return new THREE.Vector3(Math.sin(angle)*radius,y,-Math.cos(angle)*radius);}
export const easeInQuad=t=>t*t;
export const ease=t=>t*t*(3-2*t);
export function place(object,angle,radius,y,scale=1){object.position.copy(polarPosition(angle,radius,y));object.rotation.y=-THREE.MathUtils.degToRad(angle);object.scale.setScalar(scale);object.userData.base=object.position.clone();object.userData.angle=angle;return object;}
export function makeCardTexture(suit='♥',value='Q'){const canvas=document.createElement('canvas');canvas.width=256;canvas.height=356;const x=canvas.getContext('2d');x.fillStyle='#eaded4';x.fillRect(0,0,256,356);x.strokeStyle='#310608';x.lineWidth=6;x.strokeRect(6,6,244,344);x.fillStyle=suit==='♥'||suit==='♦'?'#95131c':'#090606';x.font='bold 44px Georgia';x.fillText(value,18,50);x.fillText(suit,18,96);x.font='92px Georgia';x.textAlign='center';x.fillText(suit,128,220);const texture=new THREE.CanvasTexture(canvas);texture.colorSpace=THREE.SRGBColorSpace;return texture;}
export function disposeRoot(root){const geometries=new Set(),materials=new Set();root.traverse(o=>{if(o.geometry)geometries.add(o.geometry);if(o.material)(Array.isArray(o.material)?o.material:[o.material]).forEach(m=>materials.add(m));});geometries.forEach(g=>g.dispose());materials.forEach(m=>{m.map?.dispose();m.dispose();});}

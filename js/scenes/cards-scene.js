import * as THREE from 'three';
import {around,makeCardTexture} from './utils.js';
export function createCardsScene(){const root=new THREE.Group(),cards=[],geometry=new THREE.PlaneGeometry(.3,.43),suits=['♥','♥','♥','♥','♥','♥','♦','♦','♣','♠'];for(let i=0;i<40;i++){const queen=i%10===0,suit=suits[i%10],value=queen?'Q':String(i%9+1),material=new THREE.MeshBasicMaterial({map:makeCardTexture(suit,value),side:THREE.DoubleSide});const card=around(new THREE.Mesh(geometry,material),i,40,1.8+(i%7)*.5,-1.35+(i%9)*.42);card.scale.setScalar(queen?1.55:.75+(i%4)*.12);card.userData.mode=i%9===0?'throw':i%3===0?'orbit':'float';card.userData.radius=Math.hypot(card.position.x,card.position.z);card.userData.queen=queen;root.add(card);cards.push(card);}return{root,reset(){},update(dt,elapsed,reduced){cards.forEach((card,i)=>{const phase=i*.61;if(card.userData.mode==='throw'){const cycle=(elapsed*.48+i*.17)%1,angle=card.userData.angle;const radius=THREE.MathUtils.lerp(6,-1.2,cycle*cycle);card.position.set(Math.sin(angle)*radius+(i%2?.85:-.85),card.userData.base.y+Math.sin(cycle*Math.PI)*.5,-Math.cos(angle)*radius);if(!reduced)card.rotation.set(cycle*9,cycle*12,cycle*7);}else if(card.userData.mode==='orbit'){const a=card.userData.angle+(reduced?0:elapsed*.08);card.position.set(Math.sin(a)*card.userData.radius,card.userData.base.y,-Math.cos(a)*card.userData.radius);card.rotation.y=-a;}else{card.position.copy(card.userData.base);if(!reduced)card.position.y+=Math.sin(elapsed*.45+phase)*.09;card.rotation.z=Math.sin(elapsed*.25+phase)*.2;}});},getMessage(elapsed){const t=elapsed%10;return t>.5&&t<2.5?'THE QUEEN IS WATCHING.':'';},getTint(elapsed){const t=elapsed%10;if(t>.5&&t<2.5)return .13;return 0;}};}

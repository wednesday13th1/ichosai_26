import * as THREE from 'three';
import {disposeRoot,easeInQuad,makeCardTexture,place,polarPosition} from './utils.js';
const queenLayout=[[75,4.2,.5,1.7],[185,3.4,1.4,2.2],[260,4.8,-.5,1.4],[330,3.7,2.8,1.6]];
export function createCardsScene(){const root=new THREE.Group(),cards=[],geometry=new THREE.PlaneGeometry(.3,.43),count=navigator.deviceMemory&&navigator.deviceMemory<=4?28:matchMedia('(max-width:480px)').matches?36:42;for(let i=0;i<count;i++){const queen=i<4,a=queen?queenLayout[i][0]:(i*137.5+28)%360,r=queen?queenLayout[i][1]:2.4+(i%6)*.48,y=queen?queenLayout[i][2]:(i%9<3?2.1+(i%4)*.7:i%9<5?-1.5-(i%3)*.45:-.7+(i%5)*.42),s=queen?queenLayout[i][3]:.65+(i%4)*.1,suit=i%4===0?'♠':i%3===0?'♦':'♥',card=place(new THREE.Mesh(geometry,new THREE.MeshBasicMaterial({map:makeCardTexture(suit,queen?'Q':String(i%9+1)),side:THREE.DoubleSide})),a,r,y,s);card.rotation.z=((i%7)-3)*.11;card.userData.moving=!queen&&i>=count-4;card.userData.index=i;root.add(card);cards.push(card);}const particleGeometry=new THREE.BufferGeometry(),points=[];for(let i=0;i<35;i++)points.push(...polarPosition((i*97)%360,2+(i%7)*.5,-1+(i%8)*.65).toArray());particleGeometry.setAttribute('position',new THREE.Float32BufferAttribute(points,3));root.add(new THREE.Points(particleGeometry,new THREE.PointsMaterial({color:0x95131c,size:.035,transparent:true,opacity:.28})));return{root,update(time,delta){cards.forEach(card=>{if(card.userData.moving){const t=(time/(1.2+(card.userData.index%4)*.2)+card.userData.index*.19)%1,start=polarPosition(card.userData.angle,6,card.userData.base.y),target=new THREE.Vector3(card.userData.index%2?.9:-1,(card.userData.index%4-2)*.45,.6);card.position.lerpVectors(start,target,easeInQuad(t));card.rotation.x+=delta*4;card.rotation.y+=delta*5;card.rotation.z+=delta*2;}else{card.position.y=card.userData.base.y+Math.sin(time*.45+card.userData.index)*.08;card.rotation.z+=delta*(.1+(card.userData.index%5)*.08);}});},reset(){cards.forEach(c=>c.position.copy(c.userData.base));},dispose(){disposeRoot(root);}};}

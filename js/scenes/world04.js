import * as THREE from 'three';
import {createPlayingCard,createRose,createTeaCup,createTeapot} from '../objects.js';
import {applyResponsivePriority,applyThemeToRoot,createDepthLayers,createVisitVariation,disposeRoot,microMotion,motionPreference} from './composition.js';
import {upgradeWithModel} from './asset-loader.js';
import {WORLD_CONFIGS} from './world-config.js';

export const TEA_PARALLAX=Object.freeze({far:.20,mid:.50,near:.90});
const PARALLAX_RANGE=.22;

function dessert(color=0xc98793){const group=new THREE.Group(),plate=new THREE.Mesh(new THREE.CylinderGeometry(.22,.24,.025,20),new THREE.MeshStandardMaterial({color:0xeee4d3,roughness:.6})),cake=new THREE.Mesh(new THREE.CylinderGeometry(.13,.15,.17,20),new THREE.MeshStandardMaterial({color,roughness:.72}));cake.position.y=.1;group.add(plate,cake);return group;}
export function createWorld04(){
 const root=new THREE.Group(),{background,midground,foreground}=createDepthLayers(root),animated=[],depthObjects=[],visit=createVisitVariation(3),motion=motionPreference();root.userData.definition=WORLD_CONFIGS.world04;
 const debugOcclusion=typeof location!=='undefined'&&new URLSearchParams(location.search).get('debugOcclusion')==='1';
 const teaPartyGroup=new THREE.Group();teaPartyGroup.name='TeaPartyComposition';midground.add(teaPartyGroup);
 const table=new THREE.Mesh(new THREE.CylinderGeometry(1.25,1.34,.09,48),new THREE.MeshStandardMaterial({color:0xe8ddcc,roughness:.9,transparent:true,opacity:.78}));table.name='TeaTable';table.position.set(0,-1.08,-3.5);table.scale.z=.58;teaPartyGroup.add(table);
 const pot=new THREE.Group();pot.name='TeaPotPivot';const teaPotGroup=createTeapot();teaPotGroup.rotation.y=Math.PI;pot.add(teaPotGroup);pot.position.set(.45,.48,-3.15);pot.scale.setScalar(.58);pot.userData={base:pot.position.clone(),phase:Math.random()*Math.PI*2,priority:1,depth:'mid',parallax:TEA_PARALLAX.mid*PARALLAX_RANGE};teaPartyGroup.add(pot);animated.push(pot);depthObjects.push(pot);
 const cup=new THREE.Group();cup.name='TeaCup';cup.add(createTeaCup());cup.position.set(-.45,-.08,-3.35);cup.scale.setScalar(.65);cup.userData={base:cup.position.clone(),phase:Math.random()*Math.PI*2,priority:1,depth:'mid',parallax:TEA_PARALLAX.mid*PARALLAX_RANGE};teaPartyGroup.add(cup);animated.push(cup);depthObjects.push(cup);upgradeWithModel(cup,'teacup',{scale:.8});
 [[-.9,.96,-5.8,.62],[.88,.86,-6.2,.57],[-.31,-.55,-1.25,.62],[.31,-.62,-1.05,.58]].forEach(([x,y,z,s],i)=>{const flower=createRose(i%2?0xd6a1aa:0xb97887);flower.position.set(x,y,z);flower.scale.setScalar(s);const depth=i<2?'far':'near';flower.userData={base:flower.position.clone(),baseScale:s,priority:i<2?2:1,depth,phase:Math.random()*Math.PI*2,parallax:TEA_PARALLAX[depth]*PARALLAX_RANGE,moving:i===0||i===3};(depth==='far'?background:foreground).add(flower);depthObjects.push(flower);animated.push(flower);});
 [[-.68,.25,-6.6,'♥','Q',-.38],[.68,.18,-5.5,'♠','A',.3]].forEach(([x,y,z,suit,value,rotation],i)=>{const card=createPlayingCard(suit,value);card.position.set(x,y,z);card.rotation.z=rotation;card.scale.setScalar(.35);card.userData={base:card.position.clone(),baseRotation:rotation,priority:2,depth:'far',phase:i+4,parallax:TEA_PARALLAX.far*PARALLAX_RANGE,moving:false};background.add(card);depthObjects.push(card);animated.push(card);});
 const debugCard=debugOcclusion?createPlayingCard('♦','T'):null;if(debugCard){debugCard.name='occlusion-debug-card';debugCard.position.set(-1.1,0,-5.2);debugCard.scale.setScalar(.55);debugCard.userData={base:debugCard.position.clone(),priority:1,depth:'far',phase:0,parallax:TEA_PARALLAX.far*PARALLAX_RANGE};background.add(debugCard);}
 [[-.62,-.9,-3.7,0xc98793],[.66,-1.02,-3.45,0xd6b18b]].forEach(([x,y,z,color])=>{const cake=dessert(color);cake.position.set(x,y,z);cake.scale.setScalar(.58);cake.userData={base:cake.position.clone(),priority:2,depth:'mid',phase:6,parallax:TEA_PARALLAX.mid*PARALLAX_RANGE,moving:false};teaPartyGroup.add(cake);depthObjects.push(cake);animated.push(cake);});
 const drops=[];for(let i=0;i<8;i++){const drop=new THREE.Mesh(new THREE.SphereGeometry(.024,6,5),new THREE.MeshBasicMaterial({color:0xd7ab72,transparent:true,opacity:0,depthWrite:false}));drop.userData.offset=i/8;foreground.add(drop);drops.push(drop);}
 const steamGeometry=new THREE.BufferGeometry(),steamPositions=new Float32Array(15);steamGeometry.setAttribute('position',new THREE.BufferAttribute(steamPositions,3));const steam=new THREE.Points(steamGeometry,new THREE.PointsMaterial({color:0xf5e9dc,size:.032,transparent:true,opacity:.28,depthWrite:false}));steam.name='TeaSteam';foreground.add(steam);
 const pourWorld=new THREE.Vector3(),pourLocal=new THREE.Vector3(),cupWorld=new THREE.Vector3(),cupLocal=new THREE.Vector3();let theme=null;function update(time,delta,{view={yaw:0,pitch:0},theme:nextTheme}={}){const active=nextTheme||theme||{animationSpeed:1},elapsed=visit.elapsed(time),t=elapsed*active.animationSpeed,eventPhase=motion.events?elapsed%11:0,lift=eventPhase>8&&eventPhase<10?Math.sin((eventPhase-8)/2*Math.PI):0,yaw=THREE.MathUtils.clamp(view.yaw||0,-.38,.38),pitch=THREE.MathUtils.clamp(view.pitch||0,-.28,.28),variant=visit.variant;
  depthObjects.forEach((object,i)=>{const u=object.userData,parallax=u.parallax,m=microMotion(t,{phase:u.phase||i,duration:7+(i%4),float:(u.moving?(u.depth==='near'?.02:.012):0)*motion.scale,rotate:u.moving?.009*motion.scale:0,scale:u.moving?.005*motion.scale:0});object.position.x=u.base.x-yaw*parallax;object.position.y=u.base.y-pitch*parallax+m.y;if(u.baseScale)object.scale.setScalar(u.baseScale*(variant===1?m.scale:1));});
  if(debugCard)debugCard.position.x=-1.1+((t%6)/6)*2.2;
  const potMotion=microMotion(t,{phase:pot.userData.phase,duration:7,float:.012*motion.scale,rotate:.035*motion.scale,scale:0});pot.rotation.z=potMotion.rotation;cup.position.y+=lift*.075*motion.scale;cup.rotation.z=Math.sin(t*.55+cup.userData.phase)*.025*motion.scale;
  root.updateMatrixWorld(true);teaPotGroup.userData.pourOrigin.getWorldPosition(pourWorld);pourLocal.copy(pourWorld);foreground.worldToLocal(pourLocal);drops.forEach(drop=>{const p=(eventPhase-8)/2+drop.userData.offset;if(lift>0&&variant===2){const progress=((p%1)+1)%1;drop.visible=true;drop.material.opacity=Math.sin(progress*Math.PI)*.28;drop.position.set(pourLocal.x+Math.sin(progress*Math.PI)*.02,pourLocal.y-progress*.42,pourLocal.z);}else{drop.visible=false;drop.material.opacity=0;}});cup.getWorldPosition(cupWorld);cupLocal.copy(cupWorld);foreground.worldToLocal(cupLocal);steam.position.copy(cupLocal);for(let i=0;i<5;i++){const p=(t*(variant===0?.075:.055)+i/5)%1,index=i*3;steamPositions[index]=Math.sin(t*.5+i*1.9)*(.016+i*.0015)*motion.scale;steamPositions[index+1]=.15+p*.29;steamPositions[index+2]=Math.cos(t*.28+i)*.007;}steam.material.opacity=(variant===0?.3:.2)*(active.particleOpacity??1);steam.geometry.attributes.position.needsUpdate=true;
 }
 function responsive(width,height){const spread=THREE.MathUtils.clamp(width/height/.51,.9,1.45);depthObjects.forEach(object=>{if(object.userData.originalX===undefined)object.userData.originalX=object.userData.base.x;object.userData.base.x=object.userData.originalX*spread;});applyResponsivePriority(animated,width,height);}
 return{root,update,setTheme(next){theme=next;applyThemeToRoot(root,next,'world04',{tint:.25});},responsive,reset(){visit.reset();animated.forEach(o=>o.userData.base&&o.position.copy(o.userData.base));pot.rotation.set(0,0,0);cup.rotation.set(0,0,0);drops.forEach(drop=>{drop.visible=false;drop.material.opacity=0;});},dispose(){disposeRoot(root);}};
}

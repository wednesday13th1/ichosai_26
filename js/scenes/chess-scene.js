import * as THREE from 'three';
import {createChessPiece,WonderlandColors} from '../objects.js';
import {around} from './utils.js';
function floorTexture(){const c=document.createElement('canvas');c.width=c.height=512;const x=c.getContext('2d');for(let y=0;y<8;y++)for(let i=0;i<8;i++){x.fillStyle=(i+y)%2?'#171417':'#eee4cf';x.fillRect(i*64,y*64,64,64);}const t=new THREE.CanvasTexture(c);t.wrapS=t.wrapT=THREE.RepeatWrapping;t.repeat.set(3,3);return t;}
export function createChessScene(){const root=new THREE.Group(),pieces=[];const floor=new THREE.Mesh(new THREE.PlaneGeometry(14,14),new THREE.MeshBasicMaterial({map:floorTexture(),transparent:true,opacity:.2,depthWrite:false,side:THREE.DoubleSide}));floor.rotation.x=-Math.PI/2;floor.position.y=-1.4;root.add(floor);const types=['pawn','rook','queen','king','pawn','rook','pawn','queen','king','pawn','rook','pawn'];types.forEach((type,i)=>{const color=i%3===0?WonderlandColors.gold:i%2?WonderlandColors.ink:WonderlandColors.ivory;const piece=around(createChessPiece(type,color),i+4,12,2.2+(i%4)*.8,-1.35);piece.scale.setScalar(type==='queen'?2.5:type==='king'?2.2:.9+(i%3)*.35);piece.userData.type=type;piece.userData.baseY=piece.position.y;root.add(piece);pieces.push(piece);});return{root,reset(){},update(dt,elapsed,reduced){pieces.forEach((piece,i)=>{if(piece.userData.type==='queen'&&!reduced)piece.rotation.y=Math.sin(elapsed*.16)*.16;else if(piece.userData.type==='pawn'&&!reduced)piece.position.y=piece.userData.baseY+Math.max(0,Math.sin(elapsed*.7+i)-.85)*.12;});},getMessage(){return'';}};}

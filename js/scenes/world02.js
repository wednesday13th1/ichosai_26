import * as THREE from 'three';
import {createHeart,createPlayingCard} from '../objects.js';
import {applyResponsivePriority,applyThemeToRoot,createDepthLayers,createVisitVariation,disposeRoot,microMotion,motionPreference,plane,texture} from './composition.js';
import {upgradeWithModel} from './asset-loader.js';
import {WORLD_CONFIGS} from './world-config.js';
import {discoveryPosition} from './discovery-ring.js';

function crimsonHaze(){return texture(256,256,(x,w,h)=>{const g=x.createRadialGradient(w/2,h/2,10,w/2,h/2,w/2);g.addColorStop(0,'rgba(118,15,34,.34)');g.addColorStop(.55,'rgba(87,11,24,.3)');g.addColorStop(1,'rgba(23,8,11,0)');x.fillStyle=g;x.fillRect(0,0,w,h);});}
function goldFlourish(mirrored=false){
 const root=new THREE.Group(),material=new THREE.MeshStandardMaterial({color:0xc49a4a,roughness:.56,metalness:.32}),sign=mirrored?-1:1;
 const curves=[[[0,0],[.11,.08],[.2,.02]],[[.08,.04],[.14,.15],[.22,.12]]];
 curves.forEach(points=>{const curve=new THREE.CatmullRomCurve3(points.map(([x,y])=>new THREE.Vector3(x*sign,y,0)));root.add(new THREE.Mesh(new THREE.TubeGeometry(curve,10,.006,5,false),material));});
 const heart=createHeart();heart.scale.setScalar(.16);heart.position.set(.22*sign,.12,0);heart.traverse(object=>{if(object.material){object.material=material;}});root.add(heart);return root;
}

export function createWorld02(){
 const root=new THREE.Group(),{background,midground,foreground}=createDepthLayers(root),cards=[],hearts=[],ornaments=[],animated=[],visit=createVisitVariation(3),motion=motionPreference();root.name='QueensCourtRoot';root.userData.definition=WORLD_CONFIGS.world02;
 const haze=plane(crimsonHaze(),5.5,4.1,{opacity:.74,depth:'far'});haze.position.set(0,.1,-7);background.add(haze);
 const courtLight=new THREE.PointLight(0xc49a4a,.52,7,2);courtLight.position.set(-1.4,1.8,-1.2);root.add(courtLight);
 const hero=[[-.9,1.02,-4.45,-.12,'Q'],[.88,.94,-4.7,.1,'A'],[-.94,-.64,-1.6,-.08,'10'],[.94,-.56,-1.72,.1,'K'],[0,1.45,-5.1,0,'Q']];
 hero.forEach(([x,y,z,roll,value],i)=>{const holder=new THREE.Group(),card=createPlayingCard('♥',value);holder.name=`QueenHeroCard-${i}`;holder.add(card);holder.position.set(x,y,z);holder.rotation.z=roll;holder.scale.setScalar(i===0?.62:i>1?.46:.56);holder.userData={base:holder.position.clone(),baseRotation:roll,baseScale:holder.scale.x,phase:i*.93+.4,priority:1,depth:i>1?'near':'mid',scaleAmplitude:i>1?.025:.03,speed:.18+i*.016,amplitudeX:.04+(i%2)*.018,amplitudeY:.024+(i%3)*.012};(i>1?foreground:midground).add(holder);cards.push(holder);animated.push(holder);upgradeWithModel(holder,'playingCard',{scale:holder.scale.x});});
 const sectors=[42,91,137,181,224,271,316];
 sectors.forEach((angle,i)=>{for(let detail=0;detail<2;detail++){const actual=angle+(detail?8:-5),radius=(detail?3.28:2.76)+(i%3)*.08,y=[.62,-.12,.38,.78,-.3,.48,.08][i]+detail*.26,holder=new THREE.Group(),suit=i===1&&detail?'♦':'♥',values=['A','Q','10','K','7','9','3'],card=createPlayingCard(suit,detail?String((i+4)%10):values[i]);card.rotation.y=THREE.MathUtils.degToRad(actual);holder.name=`QueenSector-${angle}-${detail}`;holder.add(card);holder.position.copy(discoveryPosition(actual,radius,y));holder.rotation.z=(i-3)*.045+(detail?.12:0);holder.scale.setScalar(detail?.21:.36+(i%2)*.035);holder.userData={base:holder.position.clone(),baseRotation:holder.rotation.z,baseScale:holder.scale.x,sector:angle,phase:6+i+detail*.37,priority:detail?3:2,depth:detail?'far':'mid',scaleAmplitude:detail?.025:.035,speed:.14+i*.009,amplitudeX:detail?.024:.042,amplitudeY:detail?.018:.032};(detail?background:midground).add(holder);cards.push(holder);animated.push(holder);}});
 [58,118,178,238,298,338].forEach((angle,i)=>{const heart=createHeart(),radius=3.15+(i%2)*.28;heart.name=`QueenHeart-${angle}`;heart.position.copy(discoveryPosition(angle,radius,[.88,-.22,.52,-.42,.7,.02][i]));heart.rotation.set(.04,THREE.MathUtils.degToRad(angle),(i-2)*.11);heart.scale.setScalar(.34+(i%3)*.08);heart.traverse(object=>{if(object.material){object.material=object.material.clone();object.material.color.setHex(i%2?0x741022:0x570b18);object.material.transparent=true;object.material.opacity=i%3===0?.18:.13;object.material.depthWrite=false;}});heart.userData={base:heart.position.clone(),baseScale:heart.scale.x,phase:i*1.14,priority:3,depth:'far'};background.add(heart);hearts.push(heart);animated.push(heart);});
 [74,164,246,326].forEach((angle,i)=>{const ornament=goldFlourish(Boolean(i%2));ornament.name=`QueenGold-${angle}`;ornament.position.copy(discoveryPosition(angle,3.42,[.32,.92,-.18,.64][i]));ornament.rotation.set(0,THREE.MathUtils.degToRad(angle),(i-1.5)*.12);ornament.scale.setScalar(.72+(i%2)*.14);ornament.userData={base:ornament.position.clone(),baseRotation:ornament.rotation.clone(),baseScale:ornament.scale.x,phase:i*1.7,priority:3,depth:'far'};background.add(ornament);ornaments.push(ornament);animated.push(ornament);});
 let theme=null;
 function update(time,delta,{theme:nextTheme}={}){const active=nextTheme||theme||{animationSpeed:1},elapsed=visit.elapsed(time),t=elapsed*active.animationSpeed,photoMoment=motion.events&&elapsed>=3&&elapsed<=5?Math.sin((elapsed-3)/2*Math.PI):0;cards.forEach((card,i)=>{const u=card.userData,m=microMotion(t,{phase:u.phase,duration:6.4+i*.31,float:.012*motion.scale,rotate:.035*motion.scale,scale:u.scaleAmplitude*motion.scale});card.position.set(u.base.x+Math.sin(t*u.speed+u.phase)*u.amplitudeX,u.base.y+Math.cos(t*u.speed*.73+u.phase)*u.amplitudeY+m.y,u.base.z+Math.sin(t*.1+u.phase)*.03);card.rotation.z=u.baseRotation+m.rotation;card.scale.setScalar(u.baseScale*m.scale*(1+photoMoment*(u.depth==='near'?.018:.008)));});hearts.forEach((heart,i)=>{const u=heart.userData,m=microMotion(t,{phase:u.phase,duration:9+i*.6,float:.02*motion.scale,rotate:.012*motion.scale,scale:.018*motion.scale});heart.position.set(u.base.x,u.base.y+m.y,u.base.z);heart.rotation.z=m.rotation;heart.scale.setScalar(u.baseScale*m.scale);});ornaments.forEach((ornament,i)=>{const u=ornament.userData,m=microMotion(t,{phase:u.phase,duration:11+i,float:.012*motion.scale,rotate:.008*motion.scale,scale:.01*motion.scale});ornament.position.set(u.base.x,u.base.y+m.y,u.base.z);ornament.rotation.set(u.baseRotation.x,u.baseRotation.y,u.baseRotation.z+m.rotation);ornament.scale.setScalar(u.baseScale*m.scale);});haze.material.opacity=.76+(active.name==='night'?.1:.03);}
 function responsive(width,height){const spread=width/height>1?1.22:width/height<.62?.92:1;cards.filter(card=>!card.userData.sector).forEach(card=>{if(card.userData.originalX===undefined)card.userData.originalX=card.userData.base.x;card.userData.base.x=card.userData.originalX*spread;});applyResponsivePriority(animated,width,height);}
 return{root,update,setTheme(next){theme=next;applyThemeToRoot(root,next,'world02',{tint:.3});},responsive,reset(){visit.reset();animated.forEach(object=>{if(object.userData.base)object.position.copy(object.userData.base);if(object.userData.baseScale)object.scale.setScalar(object.userData.baseScale);});},dispose(){disposeRoot(root);}};
}
